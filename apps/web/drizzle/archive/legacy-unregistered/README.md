# Archived, non-executable migration drafts

These files document changes that were previously left in the active migration
folder without entries in `meta/_journal.json`. Drizzle never executed them as
part of its migration chain.

Do not run or re-register these files. Migrations `0003` through `0005` in the
active folder reconcile their earlier schema/runtime effects, and active
migration `0006_registered_schema_completion` incorporates the later quotation
and HR lifecycle changes with the required data backfills.
