{
  "name": "@masarat/travel-providers",
  "version": "1.0.0",
  "description": "Travel provider contracts and stub implementations — GDS-agnostic interface layer",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
